
// non-auth actions in the future (e.g., fetching game data).

export const exampleAction = (dispatch) => {
    // This is an example of a non-auth action.
    // You can delete this later.
    dispatch({ type: 'SET_MESSAGE', payload: 'This is an example action.' });
};

// All previous signUp and logout logic has been removed from this file.